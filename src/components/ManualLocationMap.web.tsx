import React from 'react'; import { StyleSheet, Text, View } from 'react-native';
export function ManualLocationMap({ latitude, longitude }: { latitude: number | null; longitude: number | null; onChange: (latitude: number, longitude: number) => void }) { return <View style={styles.map}><Text style={styles.coordinateHint}>Web 版可直接編輯下方座標；手機版支援地圖 Marker。</Text><Text style={styles.coordinates}>{latitude?.toFixed(5) ?? '25.03300'}, {longitude?.toFixed(5) ?? '121.56500'}</Text></View>; }
const styles = StyleSheet.create({ map: { width: '100%', gap: 3, paddingVertical: 4 }, coordinateHint: { color: '#78716C', fontSize: 11 }, coordinates: { color: '#57534E', fontSize: 13, fontVariant: ['tabular-nums'] } });
