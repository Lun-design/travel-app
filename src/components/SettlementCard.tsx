import React from 'react'; import { StyleSheet, Text, View } from 'react-native'; import type { Settlement } from '@/lib/expenses-api';
export function SettlementCard({ settlements, labelFor }: { settlements: Settlement[]; labelFor: (id: string) => string }) { return <View style={styles.card}><Text style={styles.title}>AA 結算</Text>{settlements.length ? settlements.map((item, index) => <View key={`${item.from}-${item.to}-${index}`} style={styles.row}><Text style={styles.text}>{labelFor(item.from)} 應給 {labelFor(item.to)}</Text><Text style={styles.amount}>{item.currency} {item.amount.toFixed(2)}</Text></View>) : <Text style={styles.empty}>目前沒有需要結算的款項</Text>}</View>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#eff6ff', borderRadius: 16, padding: 16, gap: 10, marginBottom: 14 }, title: { fontSize: 17, fontWeight: '800', color: '#1e3a8a' }, row: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, text: { color: '#334155' }, amount: { fontWeight: '800', color: '#1d4ed8' }, empty: { color: '#64748b' } });
