import React from 'react'; import { Pressable, ScrollView, StyleSheet, Text } from 'react-native';
export function DayTabs({ days, selected, onChange }: { days: number[]; selected: number; onChange: (day: number) => void }) { return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.wrap}>{days.map((day) => <Pressable key={day} onPress={() => onChange(day)} style={[styles.tab, day === selected && styles.active]}><Text style={day === selected ? styles.activeText : undefined}>Day {day}</Text></Pressable>)}</ScrollView>; }
const styles = StyleSheet.create({ wrap: { gap: 8, paddingVertical: 10 }, tab: { paddingHorizontal: 18, paddingVertical: 10, borderRadius: 20, backgroundColor: '#e5e7eb' }, active: { backgroundColor: '#2563eb' }, activeText: { color: 'white', fontWeight: '700' } });
