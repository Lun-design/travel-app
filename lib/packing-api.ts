import { supabase } from './supabase'; import type { PackingTemplate } from './packing-utils'; import { templateItems } from './packing-utils';
export type PackingItem = { id: string; trip_id: string; category: string; name: string; item_name?: string; is_checked: boolean; is_packed?: boolean; assigned_to: string | null; created_at: string };
export function normalizePackingItem(row: any): PackingItem { return { ...row, name: row.item_name || row.name, item_name: row.item_name || row.name, is_checked: row.is_packed ?? row.is_checked ?? false, is_packed: row.is_packed ?? row.is_checked ?? false } as PackingItem; }
export async function listPackingItems(tripId: string): Promise<PackingItem[]> { const { data, error } = await supabase.from('packing_items').select('*').eq('trip_id', tripId).order('category').order('created_at'); if (error) throw error; return (data ?? []).map(normalizePackingItem); }
export async function createPackingItem(item: Pick<PackingItem, 'trip_id' | 'category' | 'name'> & Partial<Pick<PackingItem, 'assigned_to'>>): Promise<PackingItem> { const { data, error } = await supabase.from('packing_items').insert({ trip_id: item.trip_id, category: item.category, name: item.name, item_name: item.name, is_checked: false, is_packed: false, assigned_to: item.assigned_to ?? null }).select().single(); if (error) throw error; return normalizePackingItem(data); }
export async function updatePackingItem(id: string, patch: Partial<Pick<PackingItem, 'name' | 'item_name' | 'category' | 'is_checked' | 'is_packed' | 'assigned_to'>>): Promise<void> { const payload: Record<string, unknown> = {}; if (patch.name !== undefined || patch.item_name !== undefined) { const name = patch.item_name ?? patch.name; payload.name = name; payload.item_name = name; } if (patch.is_checked !== undefined || patch.is_packed !== undefined) { const packed = patch.is_packed ?? patch.is_checked; payload.is_checked = packed; payload.is_packed = packed; } if (patch.category !== undefined) payload.category = patch.category; if (patch.assigned_to !== undefined) payload.assigned_to = patch.assigned_to; const { error } = await supabase.from('packing_items').update(payload).eq('id', id); if (error) throw error; }
export async function deletePackingItem(id: string): Promise<void> { const { error } = await supabase.from('packing_items').delete().eq('id', id); if (error) throw error; }
export async function importPackingTemplate(tripId: string, template: PackingTemplate): Promise<void> { const { error } = await supabase.from('packing_items').insert(templateItems(template).map((item) => ({ ...item, item_name: item.name, is_packed: item.is_checked, trip_id: tripId }))); if (error) throw error; }
